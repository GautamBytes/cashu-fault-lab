name: Release gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  real-mint:
    name: Real mint — ${{ matrix.mint.name }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        mint:
          - name: Nutshell
            compose_file: infra/compose/nutshell.compose.yml
            url: http://127.0.0.1:3338
          - name: CDK
            compose_file: infra/compose/cdk-mint.compose.yml
            url: http://127.0.0.1:8085
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@v6
        with:
          version: 11.15.0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - name: Pin Rust toolchain
        run: |
          rustup toolchain install 1.97.0 --profile minimal
          rustup override set 1.97.0
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec turbo run build --filter=@cashu-fault-lab/scenario-runner
      - run: cargo build --manifest-path adapters/cdk/Cargo.toml
      - name: Start ${{ matrix.mint.name }} fake-wallet mint
        run: docker compose -f "${{ matrix.mint.compose_file }}" up -d
      - name: Wait for ${{ matrix.mint.name }} fake-wallet mint
        run: |
          for i in $(seq 1 60); do
            if curl --fail --silent "${{ matrix.mint.url }}/v1/info" >/dev/null 2>&1; then
              echo "${{ matrix.mint.name }} mint ready"
              exit 0
            fi
            sleep 2
          done
          echo "${{ matrix.mint.name }} mint did not become ready" && exit 1
      - name: Real mint response-loss recovery
        env:
          CFL_REAL_MINT_URL: ${{ matrix.mint.url }}
        run: pnpm --filter @cashu-fault-lab/reference-receiver exec vitest run test/docker-mint-e2e.test.ts
      - name: Prove funded cashu-ts and CDK sender fault handling against ${{ matrix.mint.name }}
        env:
          CFL_REAL_MINT_URL: ${{ matrix.mint.url }}
        run: pnpm --filter @cashu-fault-lab/scenario-runner exec vitest run test/cross-language-docker.test.ts
      - name: Stop ${{ matrix.mint.name }} fake-wallet mint
        if: always()
        run: docker compose -f "${{ matrix.mint.compose_file }}" down -v

  funded-wallet-lifecycle:
    name: Funded wallet lifecycle matrix
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@v6
        with:
          version: 11.15.0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - name: Pin Rust toolchain
        run: |
          rustup toolchain install 1.97.0 --profile minimal
          rustup override set 1.97.0
      - run: pnpm install --frozen-lockfile
      - name: Run four lifecycle lanes twice from clean volumes
        run: pnpm test:lifecycle:funded

  lightning-regtest-lifecycle:
    name: Lightning regtest wallet lifecycle
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@v6
        with:
          version: 11.15.0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Verify one real Lightning settlement through recovery
        run: pnpm test:lifecycle:regtest

  funded-wallet-doctor:
    name: Funded NIP-60 wallet doctor scenarios
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@v6
        with:
          version: 11.15.0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Run wallet-doctor scenarios against the real mint/relay/fixture stack
        run: pnpm test:doctor:funded

  release-gate:
    needs:
      - real-mint
      - funded-wallet-lifecycle
      - lightning-regtest-lifecycle
      - funded-wallet-doctor
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@v6
        with:
          version: 11.15.0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - name: Pin Rust toolchain
        run: |
          rustup toolchain install 1.97.0 --profile minimal --component rustfmt,clippy
          rustup override set 1.97.0
      - run: pnpm install --frozen-lockfile
      - run: pnpm format:check
      - run: pnpm docs:cli:check
      - run: pnpm codegen:check
      - run: pnpm openapi:validate
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm test:integration
      - run: pnpm build
      - run: pnpm test:consumer
      - run: pnpm audit --prod
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm test:browser
      - run: cargo fmt --manifest-path adapters/cdk/Cargo.toml --check
      - run: cargo clippy --manifest-path adapters/cdk/Cargo.toml --all-targets -- -D warnings
      - run: cargo test --manifest-path adapters/cdk/Cargo.toml
      - name: Confirm repository-only qualification remains blocked
        run: |
          if node apps/lab-cli/dist/bin.js matrix --profile delivery-v1 --release-policy spec/release-policy.json > /tmp/release-gate.txt 2>&1; then
            cat /tmp/release-gate.txt
            echo "repository-only qualification unexpectedly passed"
            exit 1
          fi
          cat /tmp/release-gate.txt
          grep -q "MINIMUM_QUALIFYING_PAIRS" /tmp/release-gate.txt
      - run: node apps/lab-cli/dist/bin.js matrix --profile legacy-nut18
      - run: node apps/lab-cli/dist/bin.js matrix --profile nut26-nostr
      - run: node apps/lab-cli/dist/bin.js run scenarios/crash-recovery/mint-response-lost.json --seed release
      - run: node apps/lab-cli/dist/bin.js run scenarios/concurrency/cross-transport-storm.json --seed release
