{
  "name": "crash-recovery-receiver-restart-mid-swap",
  "description": "Receiver accepts a delivery and starts redemption, then restarts before the swap completes. After clearing the mint fault, the receiver recovers via NUT-09 restore and settles without double credit.",
  "commands": [
    {
      "type": "configure_fault",
      "target": "mint",
      "rule": { "kind": "timeout_after_commit", "occurrence": 1 }
    },
    { "type": "send", "sender": "reference", "requestId": "AAECAwQFBgcICQoLDA0ODw" },
    { "type": "restart", "component": "receiver" },
    { "type": "clear_faults", "target": "mint" },
    { "type": "send", "sender": "reference", "requestId": "AAECAwQFBgcICQoLDA0ODw" },
    { "type": "assert_quiescent" }
  ]
}
