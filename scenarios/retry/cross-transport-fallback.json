{
  "name": "http-nostr-fallback",
  "description": "Drops the HTTP response so the sender falls back to Nostr transport. Verifies both transports converge on the same settled receipt.",
  "commands": [
    {
      "type": "configure_fault",
      "target": "http",
      "rule": { "kind": "drop_response", "occurrence": 1 }
    },
    { "type": "send", "sender": "reference", "requestId": "AAECAwQFBgcICQoLDA0ODw" },
    { "type": "assert_quiescent" }
  ]
}
