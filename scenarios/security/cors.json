{
  "name": "security-cors",
  "description": "Probes CORS origin enforcement. Verifies trusted origins can access the payment endpoint and attacker origins are blocked.",
  "commands": [
    {
      "type": "configure_fault",
      "target": "receiver",
      "rule": { "kind": "untrusted_origin", "occurrence": 1 }
    },
    { "type": "send", "sender": "reference", "requestId": "AAECAwQFBgcICQoLDA0ODw" },
    { "type": "assert_quiescent" }
  ]
}
