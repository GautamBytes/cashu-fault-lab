{
  "name": "security-ssrf",
  "description": "Probes that DNS rebinding to private IPs is blocked. Verifies proof material is never sent to internal network addresses.",
  "commands": [
    {
      "type": "configure_fault",
      "target": "http",
      "rule": { "kind": "dns_rebind_private", "occurrence": 1 }
    },
    { "type": "send", "sender": "reference", "requestId": "AAECAwQFBgcICQoLDA0ODw" },
    { "type": "assert_quiescent" }
  ]
}
